---
import { Icon } from "astro-icon/components";

interface Props {
    icon?: string;
}

const { icon = "lucide:chevron-right" } = Astro.props as Props;
---

<li class="flex items-center gap-2">
    <Icon name={icon} class="text-secondary" />
    <slot />
</li>
